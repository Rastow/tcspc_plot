%% main.tex
%% Copyright 2022 Robert Grzonka
%
% This work may be distributed and/or modified under the
% conditions of the LaTeX Project Public License, either version 1.3
% of this license or (at your option) any later version.
% The latest version of this license is in
%   http://www.latex-project.org/lppl.txt
% and version 1.3 or later is part of all distributions of LaTeX
% version 2005/12/01 or later.
%
% This work has the LPPL maintenance status `maintained'.
%
% The Current Maintainer of this work is Robert Grzonka.
%
% This work consists of the files main.tex and tcspc_daten-1.txt.
\documentclass[crop]{standalone}

% main font
\usepackage{fontspec}
\setmainfont{Latin Modern Roman}

% math font
\usepackage{amsmath}
\usepackage{unicode-math}
\setmathfont{Latin Modern Math}
\unimathsetup{
    math-style=ISO,
	bold-style=ISO,
	sans-style=italic,
	nabla=upright,
	partial=upright,
}

% units
\usepackage{siunitx}
\sisetup{
    unit-font-command=\symup,
    locale=DE,
    round-mode=figures,
    round-pad=false,
    round-precision=3,
}

% pictures
\usepackage{tikz}

% plots & global settings
\usepackage{pgfplots}
\usepgfplotslibrary{groupplots}
\pgfplotsset{
    compat=1.18,
    xlabel=$t$ / \unit{\nano\second},
    legend style={
        legend pos=north east,
        legend cell align=left,
        legend columns=1,
    },
    no marks,
    enlarge y limits=0.05,
    enlarge x limits=false,
    tick pos=lower,
    tick align=outside,
    extra tick style={
        grid=major,
        major grid style=dashed,
    },
    clip marker paths=true,
    clip limits=true,
}

% define some line styles
\pgfplotsset{
    data/.style={
        cycle list name=color list,
        only marks,
        mark=o,
        mark size=0.3,
    },
    fit/.style={
        black,
        thick,
        no marks,
        line join=round,
    },
    residuals/.style={
        black,
        only marks,
        mark=o,
        mark size=0.3,
    },
}

% number format
% german locale
\pgfkeys{
    /pgf/number format/.cd,
    read comma as period,
    set decimal separator={{{,}}},
    set thousands separator={\,},
}

\begin{document}
\begin{tikzpicture}
\begin{groupplot}[
    group style={
        columns=1,
        rows=2,
        xlabels at=edge bottom,
    },
    width=12cm,
    scale only axis,
]

\nextgroupplot[
    ylabel=$I$ / counts,
    ymode=log,
    ymin=1,
    height=8cm,
    try min ticks=5,
    extra description/.code={
        \node[anchor=south east,fill=white,draw] at (0.97,0.03) {
            \shortstack[l]{
                $\chi^2 = \num{0}$\\
                $a_1 = \qty{0}{\percent}$\\
                $\tau_1 = \qty{0}{\nano\second}$\\
                $a_2 = \qty{0}{\percent}$\\
                $\tau_2 = \qty{0}{\nano\second}$
            }
        };
    },
]

\addplot [data] table [x=t, y=irf, col sep=tab]{tcspc_daten-1.txt};
\addlegendentry{IRF}

\addplot [data] table [x=t, y=chcl3dec, col sep=tab]{tcspc_daten-1.txt};
\addlegendentry{Decay}

\addplot [fit] table [x=t, y=chcl3fit, col sep=tab]{tcspc_daten-1.txt};
\addlegendentry{Fit}

\nextgroupplot[
    ylabel=Residuals,
    height=2cm,
    try min ticks=3,
]

\addplot [residuals] table [x=t, y=chcl3res, col sep=tab]{tcspc_daten-1.txt};

\end{groupplot}
\end{tikzpicture}
\end{document}